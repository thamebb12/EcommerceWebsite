import { ThameShopValidators } from './thame-shop-validators';

describe('ThameShopValidators', () => {
  it('should create an instance', () => {
    expect(new ThameShopValidators()).toBeTruthy();
  });
});
